[[chap07, 스프링 부트 활용 안내]]
== 스프링 부트 활용 안내
=== {spring-boot} 애플리케이션
==== 자동구성 문제해결
==== `Environment` 혹은 `ApplicationContext` 가 시작하기 전에 재정의하기
==== `ApplicationContext` 계층 조직(부모 혹은 루트 컨텍스트 추가)
==== 비-웹애플리케이션 생성

=== 속성 및 구성
==== {build} 시 자동으로 속성 확장
==== `SpringApplication`  외부 구성
==== 애플리케이션 속성 외장파일 위치변경
==== 명령줄 인자 축약사용
==== 외부속성에 대한 YAML 사용
==== {spring} {profile} 활성화
==== 환경 의존적인 구성 변경
==== 외부속성에 대한 빌트인 옵션(built-in option) 발견

=== 내장서블릿 컨테이너
==== 애플리케이션에 서블릿, 필터 혹은 리스너 추가하기
==== HTTP  포트 변경
==== 할당되지 않은 HTTP  포트 무작위 사용
==== 실행시 HTTP 포트 발견
==== SSL 구성
==== 접근로그 구성
==== 프론트엔드 프록시 서버 이용
==== 톰캣 구성
==== 톰캣을 통한 다중 커넥터 활성화
==== 톰캣 7.x  혹은 8.0 이용
==== `@ServerEndpoint` 를 사용한 웹소켓 {endpoint} 생성
==== HTTP 응답 압축 활성화

=== {spring} MVC
==== JSON REST 서비스 작성
==== XML REST 서비스 작성
==== 잭슨(Jackson) 오브젝트매퍼(`ObjectMapper`) 재정의
==== `@ResponseBody` 랜더링 재정의
==== Multipart 파일 업로드 제어
==== {spring} MVC `DispatcherServlet` 전환
==== 기본 MVC 구성 전환
==== `ViewResolver` 재정의

=== HTTP 클라이언트
==== `RestTemplate` 가 프록시를 사용하도록 구성

=== 로깅
==== Logback 구성
==== Log4j 구성

=== 데이터 접근
==== `DataSource` 구성
==== `DataSource` 복수 정의
==== {spring-data} 리파지토리 이용
==== {spring} 구성으로부터 `@Entity` 정의 분리
==== JPA 속성 구성
==== `EntityManagerFactory` 재정의 이용
====  복수 `EntityManager` 이용
==== 전통적인 `persistence.xml` 이용
==== {spring-data} JPA 와 몽고 리파지토리 이용
==== REST {endpoint} 로 {spring-data} 리파지토리 노출

=== 데이터베이스 초기화
==== JPA 를 이용한 데이터베이스 초기화
==== Hibernate 를 이용한 데이터베이스 초기화
==== {spring} JDBC 를 이용한 데이터베이스 초기화
==== {spring} 배치 를 이용한 데이터베이스 초기화
==== 고차원 데이터베이스 마이그레이션 도구 사용
===== Flyway
===== Liquibase

=== 배치 애플리케이션
==== 구동시 {spring-batch} 작업 실행

=== {actuator}
==== {actuator} {endpoint} 의 포트 혹은 주소 변경
==== `whitelabel` 오류 페이지 재정의
==== {actuator} 과 Jersey

=== 보안
==== {spring-boot} 보안 구성 전환
==== `AuthenticationManager` 변경하고 사용자 계정 추가
==== 프록시 서버 뒤에서 실행될 때 HTTPS 활성화

=== 핫스와핑(Hot swapping)
==== 정적 컨텐츠 재적재
==== 컨테이너 재시작없이 템플릿 재적재
==== 빠른 애플리케이션 재시작

=== {build}
==== 빌드 정보 생성
==== 깃 정보 생성
==== 의존성 버전 재정의
==== Java 6 에서 실행하기

=== 전통적 배포
==== 배포가능한 war 파일 생성
==== 오래된 서블릿 컨테이너에 배포가능한 war  파일 생성
==== 기존 애플리케이션을 {spring-boot} 로 변환
==== 오래된 컨테이너(Servlet 2.5)에 war 배포
