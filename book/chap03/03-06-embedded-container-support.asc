[[boot-features-embedded-container]]
==== 내장 서블릿 컨테이너 지원
//http://docs.spring.io/spring-boot/docs/current/reference/htmlsingle/#boot-features-embedded-container
{spring-boot}는 {tomcat}, {jetty}와 {undertow} 서버를 내장형 컨테이너로 사용할 수 있다. 개발자 대부분이 스타터를 통해 완벽하게 구성된 인스턴스를 간단하게 사용할 수 있다. 내장형 서버는 기본적으로 `8080` 포트를 기준으로 한다.

[WARNING]
====
CentOS에서 {tomcat}을 사용하기로 결정했다면 임시 디렉터리에 컴파일된 JSP 및 파일 업로드를 저장하도록 해야 한다. 이 디렉터리는 {app}이 실행되는 동안 실패하게 되면 ``tmpwatch``에 의해 정리가 삭제될 가능성이 있다. 이를 피하기 위해서 `tomcat.*` 설정을 통해서 `tmpwatch` 구성을 재정의하여 삭제하지 않도록 하거나 ``server.tomcat.basedir``설정으로 내장{tomcat}이 다른 위치를 사용하도록 구성하면 된다.
====

[[boot-features-embedded-container-servlets-filters-listeners]]
===== 서블릿, 필터 와 리스너
내장형 서블릿 컨테이너를 사용할 때 서블릿, 필터와 서블릿 사양(예, ``HttpSessionListener``)에 있는 모든 리스너를 {spring} 빈 혹은 서블릿 컴포넌트 탐색으로 등록할 수 있다.

====== 서블릿, 필터와 리스너를 {spring} 빈으로 등록하기
어떤 ``Servlet``, ``Filter``혹은 서블릿 ``*Listener`` 인스턴스든지 {spring} 빈으로 내장형 컨테이너에 등록할 수 있다. 이러한 특징은 구성을 하는 동안 ``application.yml``의 값을 이용하도록 하여 특별한 혜택을 얻을 수 있다.

기본적으로 컨텍스트는 단독 서블릿이 ``/``에 대해서만 대응하도록 되어 있다. 다양한 서블릿 빈즈의 경우 빈의 이름이 경로에 대한 접두사로 사용된다. 필터는 ``/*``에 대응한다.

관례를 기반으로 한 대응만으로는 유연한 대응이 어렵지만 ``ServletRegistrationBean``, ``FilterRegistrationBean``과 ``ServletListenerRegistrationBean`` 클래스를 이용하면 완벽하게 대응 가능하다.

[[boot-features-embedded-container-context-initializer]]
===== 서블릿 컨텍스트 초기화
내장형 서블릿 컨테이너에서 서블릿 3.0+ 이상의 ``javax.servlet.ServletContainerInitializer`` 인터페이스나 스프링의 ``org.springframework.web.WebApplicationInitializer`` 인터페이스를 바로 실행할 수는 없다. 이는 war 파일을 실행하도록 설계되어 있는 서드파티 라이브러리의 전략을 {spring-boot}에서 우회하여 위험을 줄이기 위한 고의적인 설계 전략이다.

만약 {spring-boot} {app}에서 서블릿 컨텍스트 초기화를 수행해야할 상황이라면, ``org.springframework.boot.context.embedded.ServletContextInitializer`` 구현체를 빈으로 등록하면 된다. ``onStartup`` 메서드에서 제공하는 ``ServletContext``에 접근하고 필요하다면 ``WebApplicationInitializer`` 을 사용할 수 있다.

====== 서블릿, 필터와 리스너 탐색
내장형 컨테이너를 사용할 때 ``@WebServlet``, ``@WebFilter``와 ``@WebListener``를 선언한 클래스는 ``@ServletComponentScan``을 통해서 자동으로 등록된다.

[TIP]
====
``@ServletComponentScan``은 독립형 컨테이너에서는 컨테이너 구동시 발견 기작을 사용하기 때문에 효과가 없다.
====

[[boot-features-embedded-container-application-context]]
===== `EmbeddedWebApplicationContext`

[[boot-features-customizing-configurableembeddedservletcontainerfactory-directly]]
===== 내장 서블릿 컨테이너 재정의

[[boot-features-jsp-limitations]]
===== JSP 제한

* **{tomcat}**: WAR 패키징 혹은 실행가능한 WAR인 경우에는 동작한다. {executable-jar} 인 경우에는 {tomcat} 에 하드코딩 되어 있는 파일 패턴 때문에 동작하지 않는다.
* **{jetty}**:
* **{uderdow}**:

// JSP를 컴파일한 서블릿을 ``/WEB-INF/classes`` 에 위치시키는 link:https://tomcat.apache.org/tomcat-7.0-doc/jasper-howto.html[컴파일러 설정] 때문이 아닐까 생각하는데...
